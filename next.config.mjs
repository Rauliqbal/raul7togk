/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "sfveuafbpwhmzfiorbis.supabase.co",
      },
    ],
  },
};

export default nextConfig;
